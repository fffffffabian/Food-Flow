# Plantilla de variables de entorno.
# Copia este archivo a .env (que NO se sube a GitHub) y pega tu llave secreta real.
# La misma variable debe configurarse también en Vercel (Settings → Environment Variables).

SUPABASE_SERVICE_KEY=tu_llave_secreta_service_role_aqui
